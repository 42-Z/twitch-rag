/**
 * Что у проверок страницы общего: адрес сервиса и подставное окружение.
 *
 * Вынесено отдельно, потому что эти значения нужны в двух местах — в настройке
 * запуска (сервис поднимается там) и в самих проверках (по этому адресу
 * отличаются запросы страницы от запросов к реестру).
 */

/** Порт, на котором поднимается сервис для проверок. */
export const PORT = 8789;

/** Адрес сервиса так, как его видит браузер. */
export const HOST = `127.0.0.1:${PORT}`;

export const BASE_URL = `http://${HOST}`;

/** Адрес, заведомо никуда не ведущий: туда уходят подставные хранилища. */
export const DEAD = "http://127.0.0.1:9";

/** Токен владельца для проверок — не тот, что в бою. */
export const TOKEN = "e2e-token";
